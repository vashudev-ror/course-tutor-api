en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              invalid: "is invalid"
        tutor:
          attributes:
            email:
              invalid: "is invalid"
